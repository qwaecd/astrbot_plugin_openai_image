{
  "api_key": {
    "description": "OpenAI API Key",
    "type": "string",
    "default": "",
    "hint": "填写 sk- 开头的 OpenAI API Key",
    "obvious_hint": true
  },
  "admin_api_key": {
    "description": "OpenAI Admin API Key",
    "type": "string",
    "default": "",
    "hint": "用于管理员指令查询组织用量/费用；需要在 OpenAI 平台创建具备组织权限的 Admin API Key，不要填写普通项目 API Key",
    "obvious_hint": true
  },
  "whitelist_enabled": {
    "description": "启用用户白名单",
    "type": "bool",
    "default": false,
    "hint": "开启后，只有白名单中的用户/会话可以使用本插件；白名单为空时不限制"
  },
  "whitelist_users": {
    "description": "用户白名单",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": [],
    "hint": "填写允许使用本插件的 UID；也兼容填写 UMO 或 Session ID。可使用 /sid 查看 UID/UMO"
  },
  "whitelist_admin_bypass": {
    "description": "管理员无视白名单",
    "type": "bool",
    "default": true,
    "hint": "开启后 AstrBot 管理员即使不在白名单中也可以使用本插件"
  },
  "proxy": {
    "description": "代理地址",
    "type": "string",
    "default": "",
    "hint": "例如 http://127.0.0.1:7890；也可填 127.0.0.1:7890，插件会按 HTTP 代理处理；使用 TUN 模式时建议留空"
  },
  "model": {
    "description": "图像模型",
    "type": "string",
    "default": "gpt-image-1",
    "hint": "默认使用 OpenAI 图像生成模型 gpt-image-1"
  },
  "generation_mode": {
    "description": "文生图调用方式",
    "type": "string",
    "default": "responses_background",
    "enum": [
      "responses_background",
      "images_api"
    ],
    "hint": "responses_background 使用 OpenAI SDK + Responses 后台任务轮询，适合代理长连接不稳定；images_api 使用原 Images API 流式/非流式请求"
  },
  "responses_model": {
    "description": "Responses 调度模型",
    "type": "string",
    "default": "gpt-5-mini",
    "hint": "仅在 responses_background 文生图模式下使用；图像实际生成模型仍由“图像模型”配置决定"
  },
  "api_base": {
    "description": "OpenAI API Base",
    "type": "string",
    "default": "https://api.openai.com/v1",
    "hint": "一般无需修改；如使用兼容服务，可填写到 /v1 或完整 /images/generations"
  },
  "size": {
    "description": "图片尺寸",
    "type": "string",
    "default": "1024x1024",
    "enum": [
      "1024x1024",
      "1024x1536",
      "1536x1024",
      "1792x1024",
      "1024x1792",
      "auto"
    ],
    "hint": "不同模型支持的尺寸不同；gpt-image-1 推荐 1024x1024、1024x1536、1536x1024"
  },
  "quality": {
    "description": "图片质量",
    "type": "string",
    "default": "auto",
    "enum": [
      "auto",
      "low",
      "medium",
      "high",
      "standard",
      "hd"
    ],
    "hint": "gpt-image-1 支持 auto/low/medium/high；DALL-E 3 支持 standard/hd"
  },
  "output_format": {
    "description": "输出格式",
    "type": "string",
    "default": "png",
    "enum": [
      "png",
      "jpeg",
      "webp"
    ],
    "hint": "仅对 gpt-image 系列模型传递"
  },
  "stream_enabled": {
    "description": "启用流式生图",
    "type": "bool",
    "default": true,
    "hint": "仅对 gpt-image 系列模型启用；可减少长时间无响应导致的代理/网关断开"
  },
  "partial_images": {
    "description": "流式中间图数量",
    "type": "int",
    "default": 1,
    "hint": "取值 0-3；插件只发送最终图，中间图仅用于保持流式响应。每张中间图会额外消耗约 100 image output tokens"
  },
  "background_poll_interval": {
    "description": "后台生图轮询间隔（秒）",
    "type": "int",
    "default": 5,
    "hint": "仅在 responses_background 文生图模式下使用；取值会限制在 1-60 秒"
  },
  "background_poll_timeout": {
    "description": "后台生图最长等待时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "仅在 responses_background 文生图模式下使用；超过该时间仍未完成会报错"
  },
  "timeout": {
    "description": "请求超时时间（秒）",
    "type": "int",
    "default": 120,
    "hint": "生图可能较慢，建议 60-180 秒"
  }
}
